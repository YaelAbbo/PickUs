export enum MailConfigKey {
  SmtpFrom = 'SMTP_FROM',
  SmtpHost = 'SMTP_HOST',
  SmtpPort = 'SMTP_PORT',
  SmtpSecure = 'SMTP_SECURE',
  SmtpUser = 'SMTP_USER',
  SmtpPass = 'SMTP_PASS',
  FrontendUrl = 'FRONTEND_URL',
}
